services:
  screensaverkindle:
    build: .
    container_name: screensaverkindle
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      PORT: "8080"
